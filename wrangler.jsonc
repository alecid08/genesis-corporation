{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "nitza-demo-platform",
  "compatibility_date": "2026-07-01",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS"
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "nitza-demo-platform",
      // TODO: reemplazar con el database_id real una vez creada la DB
      // (wrangler d1 create nitza-demo-platform)
      "database_id": "REPLACE_WITH_D1_DATABASE_ID"
    }
  ],
  "r2_buckets": [
    {
      "binding": "TENANT_ASSETS",
      // TODO: reemplazar una vez creado el bucket
      // (wrangler r2 bucket create nitza-tenant-assets)
      "bucket_name": "nitza-tenant-assets"
    }
  ]
}
